import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
          main: '#53c4f7'
        },
        secondary: {
            main: '#ffa726'
        },
        error: {
            main: '#d32f2f'
        }
    }
})


export default theme;